apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sre-challenge-java.fullname" . }}-test-connection"
  labels:
    {{- include "sre-challenge-java.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sre-challenge-java.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
